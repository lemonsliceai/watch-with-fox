{
  "name": "couchverse-extension",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome extension for Couchverse",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "package": "npm run build && rm -f ../couchverse-extension.zip && zip -r ../couchverse-extension.zip manifest.json background.js content.js sidepanel.html styles.css dist icons"
  },
  "dependencies": {
    "esbuild": "^0.25.0",
    "livekit-client": "^2.18.2"
  }
}
